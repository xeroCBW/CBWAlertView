
Pod::Spec.new do |s|

  s.name         = "CBWAlertView"
  s.version      = "1.0.0"
  s.summary      = "A short description of CBWAlertView."
  s.homepage     = "http://EXAMPLE/CBWAlertView"
  s.license      = "MIT"
  s.author             = { "codeChenBW" => "861754186@qq.com" }
  s.platform     = :ios
  s.platform     = :ios, "7.0"
  s.source       = { :git => "https://github.com/xeroxmx/CBWAlertView.git", :tag => "1.0.0" }
  s.source_files  = "CBWAlertView"
  s.requires_arc = true

end
